{
  "name": "azooz-bot",
  "version": "1.0.0",
  "description": "WhatsApp bot with reminders",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "whatsapp",
    "bot",
    "openai",
    "reminder",
    "mongodb",
    "node-schedule"
  ],
  "author": "Your Name",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.7.2", // Or latest
    "dotenv": "^16.4.5", // Or latest
    "express": "^4.19.2", // Or latest (Using 4.19.2 in logs, let's keep it)
    "luxon": "^3.4.4", // <<<--- Added Luxon
    "mongodb": "^6.8.0", // Or latest
    "node-schedule": "^2.1.1" // Keep this version as it was stable
  },
  "engines": {
    "node": "18.x" // Keep Node 18
  }
}
